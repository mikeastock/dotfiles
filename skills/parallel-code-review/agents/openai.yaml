interface:
  display_name: "Parallel Code Review"
  short_description: "Run Fable, Grok, and Thermo reviews together"
  default_prompt: "Use $parallel-code-review to review this branch concurrently with Fable, Grok, and Thermo against one pinned base."
policy:
  allow_implicit_invocation: false
